Rule: react-doctor/js-combine-iterations
Severity: warning
Category: Performance
Count: 1

.filter().map() iterates the array twice — combine into a single loop with .reduce() or for...of

Suggestion: Combine `.map().filter()` (or similar chains) into a single pass with `.reduce()` or a `for...of` loop to avoid iterating the array twice

Files:
  src/components/book/info/BookInfoCard.tsx:114
