export type LoginFormValues = {
  email: string;
  password: string;
}

export type RegisterFormValues = {
  email: string;
  password: string;
  name: string;
  surname: string;
  birthDate: string;
  acceptedTerms: boolean;
}